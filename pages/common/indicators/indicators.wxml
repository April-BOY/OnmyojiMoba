<template name="indicatorsTemplate">
  <view class="list-box-control">
    <image src="{{icon.starUrl}}" wx:for="{{indicators}}" wx:key="{{index}}" class="indicator on"></image>
  </view>
</template>